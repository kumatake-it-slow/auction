<h1>出品商品一覧</h1>
  <ul>
    <% @items.each do |item| %>
      <li><%= link_to item.name,item %></li>
    <% end %>
  </ul>
  
<!-- 出品するのリンクをつける、HTML内にRUBY埋め込み時は%を使う -->  

<%= link_to "出品する", new_item_path %>
